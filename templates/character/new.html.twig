{% extends 'base.html.twig' %}

{% block title %}Ajouter un Personnage{% endblock %}

{% block body %}
    <div class="container form">

        <a class="btn btn-light" href="{{ path('character_index') }}">
            <span uk-icon="reply"></span> Retour aux personnages
        </a>

        <div class="icon bg-primary rounded-circle">
            <i class="fas fa-user"></i>
        </div>

        <h1>Ajouter un personnage</h1>

        {{ include('character/_form.html.twig') }}

        {{ include('_note.html.twig') }}

    </div>

{% endblock %}
